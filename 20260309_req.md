## 전체적인 작성 방법 
한글로 작성해줘.
분석하게된 코드의 위치를 기재해줘.
프로젝트의 구조를 분석해줘. 
모든 설명은 md 파일로 작성해주고. 필요하면 여러파일로 나누어서 구분해서 작성해줘.

## 전체적인 프로젝트 분석 및 설명
0. 전체적인 프로젝트 폴더 구조와 주요 모듈의 역할.

1. ggml 라이브러리 내에서 하드웨어 가속(Backend)이 추상화되는 방식.

2. Hexagon DSP(Qualcomm) 백엔드 관련 소스 코드가 위치한 경로와 주요 파일들

3. "llama-cli 실행 시 Hexagon DSP로 연산을 오프로딩하는 과정을 기술적으로 분석하고 싶어. 

4. 모델 로딩 단계에서 가중치(Weights)가 DSP 전용 메모리나 가속기가 접근 가능한 영역으로 어떻게 배치되는가?

5. ggml_backend_t 인터페이스를 통해 CPU 연산이 Hexagon 연산으로 전환되는 'Dispatch' 과정을 코드 수준에서 설명해줘."

## 그래프 빌드 및 백엔드 분할(Splitting) 분석
1. llama.cpp가 모델 전체를 보고 어떤 부분은 CPU가, 어떤 부분은 Hexagon이 담당할지 결정하는 로직은?

2. "llama.cpp의 llama_graph_compute 및 ggml_backend_sched (스케줄러)가 Hexagon DSP 백엔드를 사용할 때 그래프를 분할하는 메커니즘을 분석해줘.

3. ggml_backend_sched_split_graph 함수가 연산자(Operator)의 지원 여부에 따라 그래프를 어떻게 서브그래프로 쪼개는지 설명해줘.

4. CPU와 Hexagon 간의 데이터 의존성이 발생할 때, 중간 텐서(Tensor)가 어떤 방식으로 동기화되는가?

5. Hexagon 백엔드에서 특정 레이어나 연산(예: Softmax vs MatMul)을 거부(Reject)하여 CPU로 폴백(Fallback)시키는 기준은 무엇인가?"

## 메모리 타일링 및 VTCM 전송 메커니즘 분석
1. Hexagon DSP의 성능 극대화 요소인 VTCM 활용과 데이터 이동(DRAM ↔ VTCM)에 집중한 프롬프트입니다.

2. "Hexagon DSP의 하드웨어 제약(제한된 VTCM 용량)을 llama.cpp가 어떻게 관리하는지 분석해줘.

3. DRAM to VTCM Tiling: 거대한 가중치(Weights)와 활성화 함수(Activations) 텐서를 VTCM 크기에 맞춰 타일링(Tiling)하는 로직이 ggml-quic 또는 관련 커널에 어떻게 구현되어 있는가?

4. DMA & Pipelining: L2 캐시나 DRAM에서 VTCM으로 데이터를 복사할 때, 연산과 복사를 병렬화(Double Buffering 등)하는 구조가 있는지 코드 수준에서 확인해줘.

5. Memory Layout: Hexagon의 HVX(Hexagon Vector eXtensions) 연산에 최적화하기 위해 텐서의 레이아웃(예: 32비트 정렬, 블록화)을 어떻게 변환하는지 설명해줘."

6. 연산 결과 회수 및 CPU 동기화 분석
NPU 연산이 끝난 후 결과를 다시 메인 프로세스로 가져오는 과정을 분석합니다.

## "Hexagon DSP에서 연산이 완료된 후 결과를 CPU로 반환하는 파이프라인을 설명해줘.

1. Post-processing: VTCM에 있는 최종 출력 데이터를 다시 시스템 DRAM으로 이동시키는 과정.

2. RPC & Signaling: 패스트-RPC(FastRPC) 등을 통해 DSP가 연산 종료를 CPU에게 알리는 시그널링 방식.

3. Result Integration: llama.cpp 메인 루프에서 DSP로부터 받은 로짓(Logits) 데이터를 다음 토큰 생성을 위해 어떻게 취합(Accumulate)하는지 분석해줘."